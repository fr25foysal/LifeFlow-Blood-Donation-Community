
const PageTitle = ({title}) => {
    return (
        
        <div className="divider w-2/6 mx-auto divider-accent">
            <h2 className="text-center text-3xl font-medium uppercase">{title}</h2>
        </div>
    );
};

export default PageTitle;